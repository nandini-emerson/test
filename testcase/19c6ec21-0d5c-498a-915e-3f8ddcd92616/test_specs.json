{
  "name": "Functional - Missing API Key Handling",
  "description": "Ensures that the agent raises a ValueError when the required API key is missing for the selected LLM provider.",
  "test_type": "functional",
  "input": "Environment variables with missing OPENAI_API_KEY and LLM_PROVIDER set to 'openai'.",
  "output": "A ValueError is raised with the message indicating the missing API key.",
  "success_criteria": [
    "ValueError is raised",
    "Error message contains 'OPENAI_API_KEY is required'"
  ],
  "error_scenarios": [
    "No exception is raised when API key is missing",
    "Incorrect error message"
  ]
}