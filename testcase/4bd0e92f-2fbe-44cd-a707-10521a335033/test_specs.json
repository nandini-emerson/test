{
  "name": "Config Class Validates Google API Key",
  "description": "Checks that the Config class raises a ValueError if LLM_PROVIDER is set to 'google' but GOOGLE_API_KEY is missing.",
  "test_type": "functional",
  "input": "Environment variables with LLM_PROVIDER=google and no GOOGLE_API_KEY.",
  "output": "ValueError is raised indicating missing GOOGLE_API_KEY.",
  "success_criteria": [
    "ValueError is raised",
    "Error message contains 'GOOGLE_API_KEY is required'"
  ],
  "error_scenarios": [
    "Config initializes without raising error despite missing key"
  ]
}