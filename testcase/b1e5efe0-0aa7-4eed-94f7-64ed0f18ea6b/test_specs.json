{
  "name": "Integration - Google Provider API Key Validation",
  "description": "Ensures that the Config class raises a ValueError if the Google API key is missing when Google is the provider.",
  "test_type": "integration",
  "input": "Environment variable LLM_PROVIDER set to 'google' and GOOGLE_API_KEY unset.",
  "output": "ValueError is raised indicating missing GOOGLE_API_KEY.",
  "success_criteria": [
    "ValueError is raised",
    "Error message contains 'GOOGLE_API_KEY is required'"
  ],
  "error_scenarios": [
    "No error is raised when API key is missing",
    "Incorrect error message"
  ]
}